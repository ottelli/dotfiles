local widgets = {
    tasklist    = require("widgets.tasklist"),
    textclock   = require("widgets.textclock"),
    tray        = require("widgets.tray"),
    volume      = require("widgets.volume"),
    utils       = require("widgets.utils"),
    wifiName    = require("widgets.wifiName"),
    wifiStatus  = require("widgets.wirelessStatus"),
}

return widgets